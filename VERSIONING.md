# V7 / V8 版本變更規則

本文件是 V7 台股半自動交易決策監控的正式版本編號規則。後續所有程式、文件、GitHub commit、Cloudflare runtime 與部署驗收，均依此規則判定版本層級。

## 1. 主版本：V7 → V8

當「股池架構」或「選股架構」發生實質變化時，主版本升級。

例如：

- V7 → V8
- 正式池配額結構改變
- 股池分類方式改變
- 選股核心流程／核心評分架構改變
- A/B 線核心定義或選股主架構改變

原則：只要改變的是「怎麼建立候選池、怎麼選出正式標的」的核心架構，就屬於主版本變更。

## 2. 功能版本：V7.0 → V7.1

當股池架構、選股架構不變，只是新增、修改或刪除功能時，增加主版本後的小數第一位。

例如：

- V7.0 → V7.1
- 新增觀察池頁面
- 新增推播欄位
- 新增報表／API／健康檢查
- 修改 UI、監控顯示、輸出格式
- 刪除不再使用的功能

原則：不改變核心股池／選股架構，只改功能能力，屬於功能版本變更。

## 3. 修正版：V7.1 → V7.1.1

當目前版本架構與功能定義不變，只是修正程式錯誤、BUG、資料串接或其他程式連動問題時，增加第三段修正版號。

例如：

- V7.1 → V7.1.1
- 修正 API 403 / 401
- 修正 Cron 沒觸發
- 修正 Cloudflare / GitHub / 3Min 串接
- 修正推播未送出
- 修正欄位讀寫錯誤
- 修正程式語法、狀態判斷、去重、回復機制

原則：功能需求本身沒變，只是讓既有設計正確運作，屬於修正版。

## 4. 判定順序

每次改版前依序判斷：

1. 是否改變股池架構或選股架構？
   - 是 → 主版本升級（V7 → V8）
2. 否則，是否新增／修改／刪除功能？
   - 是 → 功能版本升級（V7.0 → V7.1）
3. 否則，是否僅修 BUG、串接、程式錯誤？
   - 是 → 修正版升級（V7.1 → V7.1.1）

## 5. 歷史版本處理

既有已部署版本名稱（例如 `7.5.33-dynamic-watchlist-12`）保留作為歷史紀錄，不回頭改名，以避免 GitHub Actions、Cloudflare 備份、部署紀錄與歷史驗收失去對應。

從正式採用本規則後的新版本開始，依上述三層規則編號。

## 6. 版本名稱範例

- `V7.1`：V7 核心股池／選股架構不變，新增一組新功能。
- `V7.1.1`：修正 V7.1 的 BUG 或串接問題。
- `V7.2`：在 V7 核心架構不變下，再新增／修改一項功能。
- `V8`：核心股池或選股架構改版。
- `V8.1`：V8 架構上新增功能。
- `V8.1.1`：修正 V8.1 的程式或連動 BUG。

## 7. 專案執行規則

- 每次改程式前，先判定本次屬於主版本、功能版本或修正版。
- Git commit、PR、Actions、Cloudflare runtime 與 PROJECT_HISTORY.md 使用同一版本號。
- 不因單純文件更新而升程式版本。
- 不以多次 commit 數量決定版本，版本只依變更性質決定。
- 若同一批變更同時包含功能新增與 BUG 修正，以較高層級的「功能版本」為準；若包含股池／選股架構變更，則以主版本為準。


## 8. 2026-09-19 名稱晉升：V7.5.33 → V8

- 使用者指定：現行 `V7.5.33-dynamic-watchlist-12` **僅在名稱／世代稱呼上晉升為 V8**。
- 本次不修改 Worker 程式、不修改選股邏輯、不修改股池、不修改 API、不修改 Cron、不修改 Cloudflare runtime version string，也不重新部署。
- 因此：
  - 專案世代／對外稱呼：**V8**
  - 現行正式 runtime 技術字串仍維持：`7.5.33-dynamic-watchlist-12`
- 後續新的功能版本編號，以 V8 為新基準，例如 V8.1；BUG 修正版例如 V8.1.1。


## 9. V8.0.1｜第11項資料品質修正（2026-09-19）

- 正式 runtime：`8.0.1-requirement11-q4-eps`。
- 本次不變更 3+3 股池架構、不變更 A/B 選股架構，屬於既有第11項財報資料／程式邏輯修正，因此使用修正版號 **V8.0.1**。
- 主要修正：Q4 單季 EPS 採 TWSE 財務比較 E 點通公開方法；Q1 前季 Q4 串接完成；EPS QoQ 僅在當季與前季均完成官方核對後計算。
- 未把本次修正升為 V8.1，因為沒有新增一個獨立功能，也沒有改選股／股池核心架構。


## 10. V8.0.2｜第26項驗收／3Min 串接修正（2026-09-19）

- 正式 runtime：`8.0.2-requirement26-acceptance`。
- 本次不改股池與選股核心架構，也不新增新的投資決策功能；修正的是第26項外部 3Min 串接的驗收狀態、readback 證據與健康檢查，因此屬修正版。
- 第26項只有在真實完整 payload POST + exact readback 後才可自動完成；舊計畫、模擬、單純 HTTP 回應均不能充數。
- 同日診斷確認 9/18 3Min 401 的根因是 Free plan 舊 endpoint 7 日自動刪除，不是選股程式本身。

## 11. V8.11.0｜PV Shadow V0.1（2026-09-25）

- Runtime：`8.11.0-pv-shadow-v0.1-log-only`。
- 本次新增獨立 Class-A 價量研究資料收集功能，未更動核心股池／選股架構，因此屬 V8 功能版本。
- `PV_SHADOW_ENABLED=false` 為預設；啟用時仍為 `LOG_ONLY`、`decisionImpact=false`，不得影響 Formal Core、推播或操作。
